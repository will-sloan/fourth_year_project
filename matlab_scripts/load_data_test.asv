x = 2;

data = load('audiogancheckpoint9.mat');

% dataSize = size(data.epochDiscriminatorSample);

discVals = data.epochDiscriminatorSample;

% 1, 2, 180
realVals = permute(discVals, [1, 3, 2]);

size(realVals(1, :, 1))

Y = realVals(1, :, 1);

L = 1:numel(Y);

size(L)

plot(L, Y(:, 1, 1));


X = [1, 2, 3, 4, 5];
